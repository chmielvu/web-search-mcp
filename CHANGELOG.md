## [Unreleased]

### Added
- Centralized tool metadata for analytics and Composio search tools in the shared catalog, so `quick_web_search`, `composio_similarlinks`, `composio_image_search`, `analytics_query`, and `analytics_report` now inherit their profile/tags/annotations from one place instead of inline `ToolAnnotations` blocks.
- Replaced the Cloud Run classifier container with the official Fastino GLiNER2 base inference path (`gliner2[local]`), kept the service on the existing 2 vCPU class, and raised memory/concurrency to fit the live model load after confirming the 1 GiB and 2 GiB revisions OOM'd during first inference.
- Reframed the subsumed TODO plan so GLiNER2 Cloud Run readiness precedes GLiNER2-dependent pipeline work, LLM backends own generative query fan-out/rewrite, FunctionGemma is no longer the primary fan-out proposal, and Qdrant/result-memory semantics are explicitly candidate-injection-only.
- Expanded the subsumed TODO implementation plan with HF Space-derived patterns for LLM fan-out scatter-gather, GLiNER2 unified entity/classification/structure schema, CDX-backed Wayback fetch fallback, and opt-in textstat-based content-quality scoring.
- Subsumed the two active TODO MCP roadmap notes into `plans/TODO/kindly-mcp-subsumed-implementation-plan-2026-06-05.md`, normalizing tool names and file paths to the current repo layout while preserving the TODO implementation intent.
- Phase 5.2 DuckDB page cache: created `src/kindly_web_search_mcp_server/cache/page_duckdb.py` implementing URL hash lookup (sha256[:32]), TTL expiry (created_at + ttl_seconds), metadata JSON roundtrip (stored in metadata_json column), and threading.Lock-protected writes (separate `.kindly/cache/page_cache.duckdb`, never shares analytics DB); refactored `page_cache.py` to thin facade + telemetry via cache/observability helpers while preserving exact `PageCache.lookup`/`store`/`get_page_cache` contract; added `KINDLY_PAGE_CACHE_DUCKDB_PATH` setting (default `.kindly/cache/page_cache.duckdb`); `tests/test_page_cache_duckdb.py` (URL-hash roundtrip, TTL, metadata, locked concurrent writes, separate-DB assertion) written and run failing first, then green. (Joint plan Task 5.2.)
- Phase 5.3 remove LanceDB semantic cache: removed all semantic lookup/write paths from `web_search` + `academic_search` in server.py; removed 3 semantic settings + lancedb_dir; removed lancedb from pyproject.toml + regenerated lock; `git rm`'d cache/{store.py,semantic_cache.py,schema.py}; cleaned cache/__init__.py + telemetry.py (metrics); added `tests/test_semantic_cache_removed.py` (runtime import forbids + settings + source scan); final rg over src+pyproject confirmed 0 runtime hits (except migration text in docs/changelog). Separate page DuckDB already landed. (Joint plan Task 5.3.)
- Phase 7.1 Qdrant result memory store: `cache/result_memory.py` with QdrantClient(":memory:") or path, collections by embed model+dim, deterministic UUIDs (query+url), payload roundtrip, age decay + entity-overlap boost, emits `result_memory.lookup`/`store`. Settings for path/enabled/weight etc. + validation. `tests/test_result_memory.py` green. (Joint plan Task 7.1.)
- Phase 7.2 inject result memory candidates into merge: virtual provider list (weight from settings, resource_type="cached", providers=["result_memory"]) inserted pre-RRF (reuses existing dedup); post-rerank survivors compared + `candidate_injected`/`candidate_survived` + store; `CandidateResult` model added. `tests/test_result_memory_injection.py` + combined orchestrator/rerank green. (Joint plan Task 7.2.)
- Phases 6.1/6.2 + 8.1-8.3 entity extraction + integration: pure-Python core (`entity/{models,default_schema,chunk,postprocess}.py` + `test_entity_core.py`; offset-preserving chunk, validation/dedup/overlap-merge/normalize, DEFAULT_*_LABELS); lazy optional GLiNER2 client (`gliner_client.py`, `entity-extraction` extra, `KINDLY_ENTITY_*` settings, to_thread, error events, no import at package load, tests pass w/o package); query entities extracted once on original (pre-rewrite), passed down to augment (never delete) must-keep terms; `entities: list[EntitySpan]|None` on `WebSearchResult` + `GetContentResponse`; extraction hooks in orchestrator (title+snippet) + fetch pipeline (markdown) with `entity.*_extracted` events; entity overlap as measured rerank feature (overlap.py, [-1,1] scorer with repo/version penalties, blended only under `KINDLY_RERANK_ENTITY_OVERLAP_ENABLED` + weight, distribution in rerank events). All entity tests + focused server/orchestrator/rerank green. (Joint plan Phases 6+8.)
- Phase 9.1 joint quality dashboard panels: updated Grafana JSON (new panels for tool profiles, result-memory injection/survival, rerank latency/quality, eval pass-rate, entity extraction latency; removed semantic); added supporting view in analytics/evals.py; `tests/test_grafana_dashboard_json.py` (parse + real event names) green. (Joint plan Task 9.1.)
- Phase 9.2 MCP eval judge metrics: `evals/judges.py` (JSON-only parsing + prompts for tool_choice_correct/argument_correctness/source_usefulness/ranking_quality + persist to DuckDB + Langfuse metadata); `evals/runner.py` (mcpevals adapter + deterministic + judge scores); `mcpevals` only under optional `eval` dep (not runtime); `tests/test_eval_judges.py` green. Judges/runner never invoked from user-facing `web_search`. (Joint plan Task 9.2.)
- Phase 10 docs + final verification: full updates to ARCHITECTURE.md (new pipeline diagram + caching + entity/result memory), CONFIGURATION.md (removed all LanceDB/semantic; added all new KINDLY_* for profiles/search/page/result/entity/rerank-overlap), TESTING.md (exact verification command blocks); CHANGELOG + .agent/CONTINUITY.md with full phase notes + provenance; ran every 10.2 step (focused pytest, ruff, compileall, --help, rg for zero legacy runtime symbols, git status clean) before the "test: verify joint quality refactor" commit. (Joint plan 10.1/10.2.)
- Phase 5.1 exact query cache LRU: added `cache/exact_lru.py` with in-memory `OrderedDict` LRU eviction and TTL expiry, and migrated the exact query cache wrapper away from LanceDB while preserving the current `lookup`/`store` server contract.
- Phase 3.1 rerank engine abstraction: added typed rerank candidate/result models, a `RerankEngine` protocol, engine registry support for `none`, `voyage`, `jina`, `gcp_cloudrun`, and `local_baseline`, plus fallback-preserving tests that keep merged-order behavior when rerank is bypassed or all providers fail.
- Phase 2.1 FastMCP tool visibility profiles: added a public tool catalog with stable annotations/tags, `KINDLY_TOOL_PROFILE` validation, and tag-gated `default`, `research`, `media`, `diagnostic`, `experimental`, and `full` profiles.
- Phase 2.2 opt-in FastMCP tool search: added `KINDLY_TOOL_SEARCH_ENABLED` (no legacy aliases); after profile selection, conditionally `mcp.add_transform(RegexSearchTransform(always_visible=["web_search", "get_content"]))`; emits `tool_surface.profile_applied` (always) and `tool_surface.search_enabled` (when on). Client-level tests confirm search/call metas and that "docs/URL fetch" / "YouTube transcript" queries surface correct underlying tools per profile. (Joint plan Task 2.2.)
- Phase 3.2 observable rerank bypass policy: added `rerank/policy.py` with typed `RerankDecision` (should_rerank/reason/query_type/candidate_count/engine_health), bypass for low count/exact literal/navigational/degraded/harmful class; wired early decision in `rerank/core.py` (preserve order on bypass); emits `rerank.eligibility|engine_selected|completed|bypassed` (and legacy search.rerank paths); updated observability helpers. Tests cover all 5 bypass reasons + event emission. (Joint plan Task 3.2.)
- Phase 4.1 deterministic rerank eval dataset + metrics: added `evals/rerank_cases.jsonl` (12 seed cases), `tests/fixtures/rerank_candidates.json`, extended `evals/metrics.py` with link-list aware MRR@K/nDCG@K/topK/domain hit + survival/duplicate/delta helpers (legacy dict+domain calls preserved for eval_schema). New `tests/test_rerank_eval_metrics.py` validates on cases + fixture. (Joint plan Task 4.1.)
- Phase 4.2 local rerank baseline: chose flashrank (light ONNX, no heavy torch); added to pyproject.toml deps; implemented `LocalBaselineRerankEngine` in engines.py (lazy import, to_thread, returns [] on missing -> preserves merged order); `get_default_model` support; `tests/test_local_rerank_engine.py` (absent gives [], mocked reorder). Combined with 4.1 harness. (Joint plan Task 4.2.)
- Takeover of abandoned Codex subagent work (plan 2026-06-03 joint): confirmed cut-off exactly after the 5 foundational commits (Phase 1.1/1.2/2.1/3.1/5.1); dispatched subagents for remaining; subagents + main completed 2.2 (tool search), 3.2 (policy), 4.1/4.2 (rerank eval+local), 5.2/5.3 (page DuckDB + semantic removal), 6+8 (entity core+GLiNER+integration+overlap), 7 (Qdrant result memory + injection), 9 (dashboards + judges), 10 (full docs + verification). All acceptance criteria now met; LanceDB/semantic fully removed from runtime; new architecture (exact LRU → entity → result memory lookup → ... → store survivors) + measured (events + DuckDB + Grafana + Langfuse judges outside hot path). See the 5 original + subagent commits. (Joint plan fully executed.)
- Shared observability event prefix constants for DuckDB persistence, including `rerank.`, `entity.`, `result_memory.`, and `eval.` event families.
- Phase 1.2 eval schema reconciliation: DuckDB eval tables now include `eval_tool_calls`, `eval_candidate_sets`, `eval_scores`, `eval_judge_calls`, and `eval_failures`, plus minimal Pydantic eval case models and deterministic metric helpers for tool-routing, latency, MRR, NDCG, and top-k domain checks.
- Joint phased implementation plan at `docs/superpowers/plans/2026-06-03-joint-observability-rerank-tools-result-memory.md`, cross-referencing the current TODO plans against live code and current FastMCP/GLiNER2/Qdrant/mcpevals docs. The plan resolves observability, rerank, tool-surface, and entity-aware result-memory contradictions with no backward-compatibility requirement.
- Phase 3.1 rerank engine abstraction: added typed rerank candidate/result models, a `RerankEngine` protocol, engine registry support for `none`, `voyage`, `jina`, `gcp_cloudrun`, and `local_baseline`, plus fallback-preserving tests that keep merged-order behavior when rerank is bypassed or all providers fail.
- Phase 2.1 FastMCP tool visibility profiles: added a public tool catalog with stable annotations/tags, `KINDLY_TOOL_PROFILE` validation, and tag-gated `default`, `research`, `media`, `diagnostic`, `experimental`, and `full` profiles.
- Phase 2.2 opt-in FastMCP tool search: added `KINDLY_TOOL_SEARCH_ENABLED` (no legacy aliases); after profile selection, conditionally `mcp.add_transform(RegexSearchTransform(always_visible=["web_search", "get_content"]))`; emits `tool_surface.profile_applied` (always) and `tool_surface.search_enabled` (when on). Client-level tests confirm search/call metas and that "docs/URL fetch" / "YouTube transcript" queries surface correct underlying tools per profile. (Joint plan Task 2.2.)
- Phase 3.2 observable rerank bypass policy: added `rerank/policy.py` with typed `RerankDecision` (should_rerank/reason/query_type/candidate_count/engine_health), bypass for low count/exact literal/navigational/degraded/harmful class; wired early decision in `rerank/core.py` (preserve order on bypass); emits `rerank.eligibility|engine_selected|completed|bypassed` (and legacy search.rerank paths); updated observability helpers. Tests cover all 5 bypass reasons + event emission. (Joint plan Task 3.2.)
- Phase 4.1 deterministic rerank eval dataset + metrics: added `evals/rerank_cases.jsonl` (12 seed cases), `tests/fixtures/rerank_candidates.json`, extended `evals/metrics.py` with link-list aware MRR@K/nDCG@K/topK/domain hit + survival/duplicate/delta helpers (legacy dict+domain calls preserved for eval_schema). New `tests/test_rerank_eval_metrics.py` validates on cases + fixture. (Joint plan Task 4.1.)
- Phase 4.2 local rerank baseline: chose flashrank (light ONNX, no heavy torch); added to pyproject.toml deps; implemented `LocalBaselineRerankEngine` in engines.py (lazy import, to_thread, returns [] on missing -> preserves merged order); `get_default_model` support; `tests/test_local_rerank_engine.py` (absent gives [], mocked reorder). Combined with 4.1 harness. (Joint plan Task 4.2.)
- Takeover of abandoned Codex subagent work (plan 2026-06-03 joint): confirmed cut-off exactly after the 5 foundational commits (Phase 1.1/1.2/2.1/3.1/5.1); dispatched subagent for remaining; sub completed 2.2 (tool search); main completed 3.2 (policy), 4.1 (rerank eval dataset+metrics), 4.2 (local baseline). Storage (5.2/5.3), entity (6-8), result memory (7), dashboards/judges (9), full docs/verif (10) remain for follow-up (lancedb/semantic files still present, no Qdrant/GLiNER injection yet). See commits 3d3e07d, 6ad3c31, f88dca5, 1c27826. Acceptance criteria partially met; core rerank/tool surface/eval substrate improved. (Joint plan post-dispatch completion.)
- Shared observability event prefix constants for DuckDB persistence, including `rerank.`, `entity.`, `result_memory.`, and `eval.` event families.
- Phase 1.2 eval schema reconciliation: DuckDB eval tables now include `eval_tool_calls`, `eval_candidate_sets`, `eval_scores`, `eval_judge_calls`, and `eval_failures`, plus minimal Pydantic eval case models and deterministic metric helpers for tool-routing, latency, MRR, NDCG, and top-k domain checks.
- Joint phased implementation plan at `docs/superpowers/plans/2026-06-03-joint-observability-rerank-tools-result-memory.md`, cross-referencing the current TODO plans against live code and current FastMCP/GLiNER2/Qdrant/mcpevals docs. The plan resolves observability, rerank, tool-surface, and entity-aware result-memory contradictions with no backward-compatibility requirement.
- Three coherent implementation plans consolidated from the observability/rerank/FastMCP/eval design notes: `plans/IN-DESIGN/observability/coherent-reranking-plan-2026-06-03.md`, `plans/IN-DESIGN/observability/coherent-fastmcp-tools-plan-2026-06-03.md`, and `plans/IN-DESIGN/observability/coherent-observability-eval-plan-2026-06-03.md`.
- Coherent MCP eval and LLM-as-judge framework plan at `plans/IN-DESIGN/observability/mcp-eval-llm-judge-frameworks-research-2026-06-03.md`, selecting `mcp-eval`/`mcpevals` as the only added MCP eval runner and DeepEval as the single role model for custom LLM-as-judge metric design over the existing DuckDB + Langfuse + Grafana stack.
- RAG/MCP evaluation framework addendum at `plans/observability/rag-mcp-eval-frameworks-addendum-2026-06-03.md`, comparing Ragas, DeepEval, TruLens, Phoenix, Promptfoo, mcp-eval, DeepEval MCP metrics, and MCP research benchmarks, with a recommended DuckDB + DeepEval + mcp-eval stack for this repo.
- Critical analysis report at `plans/observability/critical-analysis-fastmcp-rerank-tool-strategy-2026-06-03.md` validating the aggregate observability/rerank/FastMCP strategy as plan-only work, revising TEI-only custom reranker guidance toward a benchmark-first Infinity/TEI/FastEmbed/FlashRank bakeoff, confirming FastMCP/DuckDB/Flock constraints from current docs, and reprioritizing active GLiNER/GraphQL/Crawl4AI plans.
- **Real in-depth research (benchmarks + explicit open questions on reranking libs/models) + rewrite of the two criticized sections in the aggregated observability report** (per user explicit constraints: "PLEASE DO REAL RESEARCH BEFORE YOU REWRITE ANYTHING!", "please use benchmarks!", "please fucking ask open questions on the reranking libraries!", "The GCS section is USELESS! ... explore and review RERANKER MODELS ... compare them based on speed + models ...", "there is nothing on Code mode, and the exploration of fastMCP advanced/new features is shallow! Please do in-depth...", and full chain of feedback). Stopped before any edit; executed sequence of web_search (BEIR/MTEB nDCG 2025/2026 for jina-v3/bge-v2-m3/mxbai/gte/Ettin/MiniLM + Voyage claims; FlashRank vs rerankers vs TEI vs FlagEmbedding vs sentence-transformers CrossEncoder latency/speed for short docs/RAG/web snippets/CPU/GPU/Cloud Run; "rerank" ("hurt" OR "made worse" OR "no improvement" OR "bypass" OR "test on your data" OR "open problem" OR tradeoff) RAG/"web search"/snippets), web_fetch (gofastmcp llms.txt + /code-mode, FlashRank github, Cloudflare Code Mode blogs https://blog.cloudflare.com/code-mode/ + /code-mode-mcp/, TDS "Rerankers Aren't Magic" empirical grids on exact failure cases), x_keyword_search (recent practitioner reranker/FlashRank/TEI latency/benchmark/Cloud Run/"worse"/"hurt" since 2025). Read current report + subagent artifacts (GCS guide, CodeMode deep-dive) + code (rerank/core.py:207 short snippet formatting, settings rerank_*, server.py ~17 tools no transforms yet) + pyproject (fastmcp>=2, resolved 3.x). Cross-checked vs 48h (voyage 6.62s/18.18s p95, 155 events, Jina also slow per user, core dominant long-tail test-only, "value unproven").
  - **Rerank §2 replacement (now evidence-based, not "TEI unsupported")**: Added full models table (jina-v3 61.94 SOTA BEIR nDCG@10 [web:0][web:8], bge-v2-m3 ~56.5 [web:1][web:5], mxbai variants, MiniLM, Ettin 17M/32M beating larger with speed [web:5]); libs comparison table (FlashRank ultra-lite 4MB/34MB no-torch ONNX CPU ~30ms pract [web:15][web:17][post:3], rerankers unifier, TEI Rust first-class cross for bge/jina ready Cloud Run images <500ms CPU p95 target / <<100 L4, sentence/Flag baselines); real latency numbers (31ms overhead negligible in RAG trace [web:21], 30ms FlashRank X, public variance); explicit open questions list ("does rerank ever hurt on short noisy web snippets?" — yes per TDS grids + LinkedIn/BGE-small cases + "not always silver bullet" + "test on your data" [web:30][web:34][TDS]; "when bypass?", "real p95 for our exact short title+snippet N=20-50 on FlashRank vs TEI Cloud Run?", "quant impact?", "Cloud Run cold-start realities?"); supported recs (FlashRank CPU light-path as P0 fast default/"cheap insurance" for latency symptom + low risk; retain gcp/TEI client+guide for deep when harness proves lift; hybrid + experiments/gold+LLM-judge mandatory first per "test on data" + obs value gap; update settings/dispatch); risks honest; synergies with §5 judges + active gliner/GraphQL. GCS deploy/client/guide kept as landing for custom (refined not replaced). No Exa.
  - **CodeMode/FastMCP §3 in-depth replacement**: Added primary CF evidence (99.9% token reduction for 2500+ endpoints: 1.17M → ~1k fixed; V8 Dynamic Worker Loader ms-start disposable cheap no containers; bindings no-key-leak; search+execute; agent writes JS against typed OpenAPI in sandbox then chains real calls in *one* execute — only final back; prod MCP server + client SDK in Goose/Claude; 81% in complex task) [web:5][web:6 CF fetches]; concrete "what agent would write" Python example for *our* primitives (web_search + conditional batch_get_content + custom merge/evidence pack inside execute — no N context bloat); full layering (visibility/profiles + ToolSearch/PromptsAsTools first/always for universal clients + no py bias; CodeMode *opt-in* for coding agents needing dynamic chains); wiring (tags on all @mcp.tool, settings KINDLY_TOOL_PROFILE + ENABLE_CODE_MODE, post-register add_transforms, guarded import, sandbox tuned to our timeouts, execute_desc seeding); how solves obs exactly ("many not used" via hide + active search; agentic hyperfocus via no flat list + sandboxed goal-directed code; strategic via middleware still firing inside proxy + measurement); risks/gotchas with CF context (experimental, py bias good for our users but note, limits on browser, debug via result, security isolated); other capabilities (Sampling/Elicitation/Tasks/ResponseLimiting); current state vs server.py confirmed; practical diffs enhanced. Builds on (does not duplicate) subagent deep-dive + CF primary.
  - All per "DO REAL RESEARCH BEFORE REWRITE", use of benchmarks, open questions on libs, in-depth CodeMode (prod usage/CF examples/gotchas vs lighter features), active-plans-only scope, Exa removal, "two agents" drain, v3 coherence. Report now coherent detailed scannable with tables/fences/cites. CHANGELOG discipline + AGENTS.md followed. Subagents monitored/completed earlier.
- (Continuing prior Added items for this session: FastMCP deep research, GCS client landing + guide, LLM-judge full, practitioner 10-rec, active plans value vs 48h only, "two agents" resolution via get/wait/read of 5 IDs, v3 full rewrite for readability, etc. See below for full list.)

  - Fetched/analyzed all via web_fetch + web_search ([web:0], [web:1], [web:2] etc.) + validated vs installed fastmcp==3.2.4 source in .venv (exact CodeMode(CatalogTransform), MontySandboxProvider, discovery factories, call_tool proxy behavior, etc.).
  - Explained CodeMode: meta-tools (search/get_schema/execute in sandbox), 3-stage discovery (tokens vs roundtrips tradeoff), sandbox limits (duration/memory via Monty; requires [code-mode] extra), custom discovery (GetTags etc.), no direct side effects.
  - Applicability critical review for kindly: agents can write small Python orchestrations calling *only* primitives (web_search/get_content etc.) inside execute -- huge context savings + fewer LLM turns for coding agents (Claude Code/Cursor excel at this). Pros: leverages Python strength of target users, middleware/steering applies inside sandbox for free, scales w/ catalog growth. Cons: Python bias (bad for non-coding agents), experimental, debug harder, overhead for simple flows, philosophy tension w/ "external control". 
  - Current usage: rich *custom* middleware for steering (expensive_tool_protection w/ first-call block+SessionTracker+ToolError guidance; DynamicGuidanceMiddleware injecting agent_guidance/suggested_next_tools/suggested_prompts into results; differentiated rates; see src/kindly_web_search_mcp_server/middleware/* + tests/test_*_middleware*.py), no transforms (mcp_compat.py homegrown+unused), tags only on prompts, no Visibility/CodeMode/ToolSearch, Context used for info() but not full progress/sampling/elicitation. 16 tools + prompts/resources.
  - Practical recs (aligned with steering recommendations in codebase + active plans): layer (profiles/Visibility default + ToolSearch + Prompts/ResourcesAsTools + *opt-in* CodeMode via env); add tags to tools (for GetTags/Search/Visibility); wire CodeMode w/ GetTags+Search(detailed)+GetSchemas, sandbox limits tuned to our _resolve_tool_total_timeout_seconds() + browser headroom; middleware layers naturally (execute proxies full pipeline); primitives = discovery/fetch focused. Risks (experimental flag, Python bias for non-natives, security mitigated by isolation+proxy, debug via enriched errors) + how combo + per-session solves "many not used" (hides surface; lazy discovery forces intent) + hyperfocus (no flat list to latch on; guidance inside).
  - Code-ready diffs + wiring examples for server.py (also pyproject bump/optional, settings profile, CHANGELOG/docs). Full report: plans/research/FastMCP-CodeMode-Transforms-Visibility-Steering-Deep-Dive-2026-06-02.md. Subagent task complete. (See also recent Unreleased FastMCP 3.x notes from prior subagent obs research. Note: per user instruction, no plans under plans/Done/ were read/used for final synthesis.)
- (Prior items from this run and before; see full history.)
- Final deeper research pass (post-idle subagent retrieval + direct web_fetch on Cloudflare Code Mode blog + Flock docs + TEI guide + web_search on Flock examples): Incorporated full practitioner playbook (10 recs + Cloudflare 99.9% / 1k-token fixed footprint for 2500 endpoints via search+execute in V8 isolates + Worker Loader + bindings; evidence from real cases/SEPs), LLM-as-Judge (prompts, Flock SQL examples from arXiv/docs [llm_filter/llm_complete on papers + hybrid rerank], Python batch + Grafana/Langfuse, cost controls, gold validation), detailed GCS (TEI Cloud Run exact gcloud + GPU + client + DuckDB queries + targets), CodeMode/Flock direct highlights (99.9% numbers + exact SQL + "LLMs better at writing code"), plans portfolio review limited exclusively to active plans (per user instruction: **no plans under `plans/Done/` were read or used** in the final assessment; focused on GraphQL-tuning.md + gliner/ + playright/ with 48h metric mappings + concrete nexts; all prior Done/ citations excised from report). Usage correction. All in updated aggregated report + this changelog discipline. Subagents + direct calls executed to address all 7 points with concrete depth.
- **"two agents are still running" resolved (user query)**: On explicit report, listed .grok sessions/subagents/ (9 tracked under parent 019e8bf3-..., 5 recent 019e8c3* child sessions all with summary.json), read metas (identified: practitioner 019e8c3e-391f-7fe2-a36c-94e79ad2ab8d 171s/43 calls "Deeper high-quality practitioner advice...", LLM-judge 019e8c3e-1d93-70d3-a595-3c4a6f631419 275s/88 calls, GCS 019e8c3d-fe5d... 348s/48 calls + live edits to rerank/gcp_cloudrun.py + settings/core/__init__, plans-portfolio 019e8c3e-53ce... 147s/49 calls, CodeMode 019e8c3d-db46... 395s/87 calls), executed get_command_or_subagent_output (block=true, 120s/60s timeouts) + wait_commands_or_subagents (wait_all on all 5 IDs). All completed (exit 0), full outputs (playbooks, prompts/SQL, guides, portfolio tables with 48h mappings) retrieved into context and integrated into aggregated report (header update + practitioner 10-rec playbook + LLM-judge full design/prompts/Flock + plans 8.5 cross-val note). GCS client (gcp_cloudrun_rerank) verified (import OK, ruff clean after F401 noqa). No other agents/background pending. Report is now the complete single source per original request. (All per AGENTS.md changelog + scope rules.)
- **Report rewrite for coherence, detail, and formatting (user query)**: The aggregated report (`plans/observability/aggregated-findings-recommendations-fastmcp-rerank-tool-strategy-2026-06-03.md`, previously ~409 lines) had become unreadable due to accumulated search_replace insertions (duplicate/out-of-order headings like "8. v2 Deep Dive" mixed with old "1. Consolidated...", walls of raw pasted subagent output, inconsistent structure, stray placeholders). Performed full rewrite via `write` tool:
  - New clean logical structure: Metadata + scope (active plans only, explicit "no Done/"), concise Executive Summary with 48h numbers + two focus areas + top recs, numbered main sections (1. 48h Snapshot, 2. Rerank Latency + detailed GCS, 3. Strategic Tool Use + FastMCP/CodeMode layering, 4. Practitioner Playbook as 10 clean prioritized recs with Why/How/Measure, 5. LLM-as-Judge with exact prompts + Flock SQL + sketches + impl order, 6. Active Plans Value as structured assessment + table-like mappings, 7. Prioritized Roadmap P0/P1/P2 with acceptance criteria, 8. Risks/Critique, 9. Next Steps + Appendix with subagent IDs/provenance/file:line).
  - Formatting: Consistent H1-H3, short paragraphs, **bold** actions, proper fenced code blocks (gcloud, prompts, SQL, wiring diffs, Python sketches), tables for roadmap/comparisons where helpful, scannable numbered/bulleted lists instead of monolithic text.
  - Preserved 100% of technical depth, all 5 subagent IDs + durations/calls, 48h metrics, GCS client details (already landed), exact prompts/Flock, 10-rec playbook, 99.9% Cloudflare numbers, active-plans-only scope, citations, and every actionable rec.
  - Line count now ~506 (modest increase from better organization + explicit structure; far more usable).
  - Verified post-write: clean heading outline (sequential 1-9, no dups), top reads cleanly, all prior v2 depth + "two agents" resolution note retained at top.
  - This directly fulfills the request while keeping the report the single source of truth. (See also the v3 note and "This document (v3 rewrite)" section in the report itself.)

### Removed
- Phase 5.3: LanceDB semantic cache and all related code (Joint plan Task 5.3):
  - `git rm src/kindly_web_search_mcp_server/cache/{store.py,semantic_cache.py,schema.py}`
  - Removed semantic cache lookup + background write paths from both `web_search` and `academic_search` in server.py (exact LRU remains).
  - Removed the three settings (`lancedb_dir`, `semantic_cache_enabled`, `semantic_cache_min_score`) + leftover __post_init__ validation from settings.py.
  - Removed `lancedb>=0.25.0` from pyproject.toml dependencies; `uv lock` regenerated (lancedb no longer resolved).
  - Cleaned `cache/__init__.py` (docstring, removed schema/semantic/store imports + exports; retained content_type/exact/page for now).
  - Purged `get_semantic_cache_metrics`, `record_semantic_cache_lookup`, associated globals, metric names, and export list entries from telemetry.py.
  - Updated server status resource and removed now-unused classify_content_type import.
  - `tests/test_semantic_cache_removed.py` + `rg "lancedb|semantic_cache" src pyproject.toml` (run as part of verification) confirm zero runtime references outside the three deleted files and migration notes in changelog/docs.
  - Uses exact plan commit message: "refactor: remove LanceDB semantic cache".
  - (No aliases kept; no cache_hit="semantic" path remains; per compatibility policy.)

### Changed
- FastMCP dependency floor bumped to `fastmcp>=3.4.0` after checking the latest stable package metadata and current FastMCP docs for the tool/resource/prompt APIs used by the merged plan.
- (No breaking; additive steering layers.)

## [0.1.0] - Previous (truncated...)
