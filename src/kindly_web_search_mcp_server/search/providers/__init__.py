"""Provider adapters for web search.

Each module exposes an async ``search_<name>`` function that implements a single
search backend. Definitions and adapters are registered in
``search/provider_registry.py``.
"""
